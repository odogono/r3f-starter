#!/bin/sh

set -eu

echo 'Running format and lint checks...'

bun run format
bun run lint

if ! git diff --quiet -- src; then
  echo
  echo 'Formatting changed files under src/. Review, stage the updates, and commit again.'
  git diff --name-only -- src
  exit 1
fi
